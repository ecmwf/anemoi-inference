post_processors:
  - accumulate_from_start_of_forecast
checkpoint: "{CHECKPOINT}"
input:
  grib: "{TMP_DIR}/input.grib"
output:
  grib:
      path: "{TMP_DIR}/output.grib"
      encoding:
        stream: oper
        class: ai
        type: fc
